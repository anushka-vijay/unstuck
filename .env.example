# Copy this file to .env.local and paste your Anthropic API key.
# Without a key, the app runs in demo mode with hardcoded fallbacks
# (useful for stage backup — the live demo never crashes).
VITE_ANTHROPIC_API_KEY=sk-ant-...
